apiVersion: v1
kind: Service
metadata:
  name: {{ include "articache.fullname" . }}
  labels:
    {{- include "articache.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "articache.selectorLabels" . | nindent 4 }}
